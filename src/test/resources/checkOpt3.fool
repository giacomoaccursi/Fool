let
  class B (b:bool) {
    fun d:int () b;
  }

  class T extends B (t:bool) {
      fun r:int () t;
  }

  var tt:T = new T(true, true);
  var bb:B = new B(false);

in print(if (1==2) then {tt} else {bb});
